/**
 * @module jobService
 * @description Datová vrstva pro entitu Zakázka (Servisní výjezd).
 * Řeší komplexní dotazy pro spojení zakázky se stroji a historií.
 */

import { supabase } from '../api/supabaseClient';

export const createJob = async (jobData) => {
  // TODO: Implement creation
};